.DS_Store

# .claude is a mount of the context-flow framework, its own repo:
# https://github.com/AlfoldiMate/context-flow — a symlink, never tracked here
# (bare entry: dir patterns do not match symlinks)
.claude
.claude.premount-backup/

# Rust
target/

# local embedding model cache
.fastembed_cache/

# surreal sql shell history, dropped wherever the CLI is run
history.txt
