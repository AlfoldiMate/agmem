.DS_Store

# ast-grep custom-language registry; libraryPath is an absolute path into
# ~/.cache/ctx-flow, so it is machine-local (/ast-grep-it writes it)
sgconfig.yml

# .claude is a symlink the bare-worktree layout places on apply — it points
# at the ctx-flow checkout at the root, never tracked here (a symlink does
# not match a `.claude/` dir pattern, so no trailing slash)
.claude

# Rust
target/

# local embedding model cache
.fastembed_cache/

# #133 candidate recordings: ~3 MB per candidate, regenerated on demand
crates/agmem-server/tests/fixtures/eval/candidates/
# #133 per-candidate run logs; the numbers that matter go in embed-models.md
docs/eval/embed-models/*/

# surreal sql shell history, dropped wherever the CLI is run
history.txt
